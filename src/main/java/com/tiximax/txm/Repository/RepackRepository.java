package com.tiximax.txm.Repository;

import com.tiximax.txm.Entity.Repack;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RepackRepository extends JpaRepository<Repack, Long> {
//    long countByCustomerCustomerId(Long customerId);
//
//    boolean existsByRepackCode(String repackCode);
}
